# v0.1.1 — CI: Build & Test Improvements

**Released:** 2026-05-07

---

## CI and Tests

### Release Pipeline

- **[Attached VideoDownloader.exe to GitHub Release]**: Release workflow now
  builds the Windows executable via PyInstaller and uploads it as a release asset

### Test Runner

- **[Graceful skip when no tests collected]**: pytest exit code 5 is now
  treated as success so CI does not fail on an empty test suite

---

## Versioning

> `Patch` bump: CI fixes only — no new features or breaking changes.
